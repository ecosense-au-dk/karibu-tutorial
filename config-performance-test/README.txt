This configuration is used for performance testing at AU and does not make much sense
outside our local environment.

The MQ is the PundaMQ01 and 02 cluster on MarkII

The DB is BigMongo on Rundo, database collection perform-test

- Henrik Bærbak